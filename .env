# Set VITE_API_BASE_URL only if using a non-standard API domain.
# For local dev, getApiBaseUrl() dynamically defaults to http://localhost:5000.
# For production/deployment, getApiBaseUrl() automatically resolves production endpoints and Vercel rewrites.
# VITE_API_BASE_URL=
